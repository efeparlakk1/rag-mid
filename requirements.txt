# ── PDF Parsing ───────────────────────────────────────────────
pymupdf4llm>=0.0.17        # PDF -> Markdown (PyMuPDF tabanlı)

# ── Chunking ──────────────────────────────────────────────────
langchain-text-splitters>=0.3.0   # MarkdownHeaderTextSplitter + Recursive
tiktoken>=0.7.0                    # token-bazlı uzunluk ölçümü

# ── Embedding + Reranker ──────────────────────────────────────
FlagEmbedding>=1.3.0       # BGE-M3 (dense+sparse) ve bge-reranker-v2-m3
                           # (torch + transformers'ı kendisi çeker; CUDA wheel'i otomatik)

# ── Vektör Deposu ─────────────────────────────────────────────
qdrant-client>=1.12.0      # embedded (path=) modunda hibrit (dense+sparse) Query API

# ── LLM İstemcisi ─────────────────────────────────────────────
ollama>=0.3.0              # local Qwen2.5 ile konuşma

# ── Arayüz & Gösterge ─────────────────────────────────────────
tqdm>=4.66.0              # ingest sırasında renkli ilerleme çubuğu
streamlit>=1.38.0         # futuristik web sohbet arayüzü
